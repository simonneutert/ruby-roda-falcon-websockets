{
  "solargraph.formatting": true, // use the internal language server (see below)
  "ruby.lint": {
    "rubocop": {
      "useBundler": true // enable rubocop via bundler
    },
    "reek": {
      "useBundler": true // enable reek via bundler
    }
  },
  "ruby.format": "rubocop",
  "ruby.useLanguageServer": true,
  "solargraph.useBundler": true, // use rubocop for formatting
  "editor.formatOnSave": true
}
